# random-counter-injection
Python project demonstrating low‑level interaction with Windows Universal C Runtime (ucrtbase.dll) using CFFI. Builds custom va_list buffers to call __stdio_common_vfprintf and prints formatted output. Includes Frida hooks to intercept and modify arguments at runtime for learning and experimentation.
